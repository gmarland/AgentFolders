{
  "name": "agent-context",
  "displayName": "Agent Context",
  "description": "Attach external reference folders to your workspace for agent and developer context",
  "version": "0.0.4",
  "engines": {
    "vscode": "^1.85.0"
  },
  "categories": [
    "Other"
  ],
  "activationEvents": [
    "onView:agentFolders"
  ],
  "main": "./out/extension.js",
  "contributes": {
    "viewsContainers": {
      "activitybar": [
        {
          "id": "agentFoldersSidebar",
          "title": "Agent Context",
          "icon": "media/symlink-folders.svg"
        }
      ]
    },
    "commands": [
      {
        "command": "agentFolders.addFolder",
        "title": "Add Context Folder",
        "icon": "$(add)"
      },
      {
        "command": "agentFolders.removeFolder",
        "title": "Remove Context Folder",
        "icon": "$(trash)"
      },
      {
        "command": "agentFolders.refresh",
        "title": "Refresh",
        "icon": "$(refresh)"
      },
      {
        "command": "agentFolders.openFolderDirectory",
        "title": "Open Agent Context Directory",
        "icon": "$(folder-opened)"
      },
      {
        "command": "agentFolders.revealOriginalFolder",
        "title": "Reveal Original Folder",
        "icon": "$(file-symlink-directory)"
      },
      {
        "command": "agentFolders.editDescription",
        "title": "Edit Description",
        "icon": "$(edit)"
      }
    ],
    "views": {
      "agentFoldersSidebar": [
        {
          "id": "agentFolders",
          "name": "Attached Context"
        }
      ]
    },
    "viewsWelcome": [
      {
        "view": "agentFolders",
        "contents": "No folders are attached yet.\n[Add Context Folder](command:agentFolders.addFolder)"
      }
    ],
    "menus": {
      "view/title": [
        {
          "command": "agentFolders.addFolder",
          "when": "view == agentFolders",
          "group": "navigation"
        },
        {
          "command": "agentFolders.refresh",
          "when": "view == agentFolders",
          "group": "navigation"
        },
        {
          "command": "agentFolders.openFolderDirectory",
          "when": "view == agentFolders",
          "group": "navigation"
        }
      ],
      "view/item/context": [
        {
          "command": "agentFolders.removeFolder",
          "when": "view == agentFolders && viewItem == agentFolderItem",
          "group": "inline"
        },
        {
          "command": "agentFolders.editDescription",
          "when": "view == agentFolders && viewItem == agentFolderItem",
          "group": "inline"
        },
        {
          "command": "agentFolders.revealOriginalFolder",
          "when": "view == agentFolders && viewItem == agentFolderItem",
          "group": "inline"
        }
      ]
    },
    "configuration": {
      "title": "Agent Context",
      "properties": {
        "agentContext.targetFolder": {
          "type": "string",
          "default": ".examples",
          "description": "The folder within the workspace where agent context symlinks will be created"
        },
        "agentContext.updateAgentInstructions": {
          "type": "boolean",
          "default": true,
          "description": "Automatically update the shared instructions file when folders are added or removed"
        },
        "agentContext.instructionsFile": {
          "type": "string",
          "default": "AgentContext.AGENTS.md",
          "description": "Relative path within the workspace to the generated Agent Context instructions file to update"
        }
      }
    }
  },
  "scripts": {
    "vscode:prepublish": "npm run compile",
    "compile": "tsc -p ./",
    "watch": "tsc -watch -p ./",
    "lint": "eslint src --ext ts",
    "test": "npm run compile && node --test test/**/*.test.mjs"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "@types/vscode": "^1.85.0",
    "typescript": "^5.3.0"
  }
}
