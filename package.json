{
  "name": "agent-context",
  "displayName": "Agent Context",
  "description": "Attach external reference folders to your workspace for agent and developer context",
  "publisher": "dotstorming",
  "icon": "media/logo.png",
  "version": "0.0.4",
  "engines": {
    "vscode": "^1.85.0"
  },
  "categories": [
    "Other"
  ],
  "activationEvents": [
    "onView:agentContext"
  ],
  "main": "./out/extension.js",
  "contributes": {
    "viewsContainers": {
      "activitybar": [
        {
          "id": "agentContextSidebar",
          "title": "Agent Context",
          "icon": "media/symlink-folders.svg"
        }
      ]
    },
    "commands": [
      {
        "command": "agentContext.addFolder",
        "title": "Add Context Folder",
        "icon": "$(add)"
      },
      {
        "command": "agentContext.removeFolder",
        "title": "Remove Context Folder",
        "icon": "$(trash)"
      },
      {
        "command": "agentContext.refresh",
        "title": "Refresh",
        "icon": "$(refresh)"
      },
      {
        "command": "agentContext.openFolderDirectory",
        "title": "Open Agent Context Directory",
        "icon": "$(folder-opened)"
      },
      {
        "command": "agentContext.revealOriginalFolder",
        "title": "Reveal Original Folder",
        "icon": "$(file-symlink-directory)"
      },
      {
        "command": "agentContext.editDescription",
        "title": "Edit Description",
        "icon": "$(edit)"
      }
    ],
    "views": {
      "agentContextSidebar": [
        {
          "id": "agentContext",
          "name": "Attached Context"
        }
      ]
    },
    "viewsWelcome": [
      {
        "view": "agentContext",
        "contents": "No folders are attached yet.\n[Add Context Folder](command:agentContext.addFolder)"
      }
    ],
    "menus": {
      "view/title": [
        {
          "command": "agentContext.addFolder",
          "when": "view == agentContext",
          "group": "navigation"
        },
        {
          "command": "agentContext.refresh",
          "when": "view == agentContext",
          "group": "navigation"
        },
        {
          "command": "agentContext.openFolderDirectory",
          "when": "view == agentContext",
          "group": "navigation"
        }
      ],
      "view/item/context": [
        {
          "command": "agentContext.removeFolder",
          "when": "view == agentContext && viewItem == agentContextItem",
          "group": "inline"
        },
        {
          "command": "agentContext.editDescription",
          "when": "view == agentContext && viewItem == agentContextItem",
          "group": "inline"
        },
        {
          "command": "agentContext.revealOriginalFolder",
          "when": "view == agentContext && viewItem == agentContextItem",
          "group": "inline"
        }
      ]
    },
    "configuration": {
      "title": "Agent Context",
      "properties": {
        "agentContext.targetFolder": {
          "type": "string",
          "default": ".examples",
          "description": "The folder within the workspace where agent context symlinks will be created"
        },
        "agentContext.updateAgentInstructions": {
          "type": "boolean",
          "default": true,
          "description": "Automatically update the shared instructions file when folders are added or removed"
        },
        "agentContext.instructionsFile": {
          "type": "string",
          "default": "AgentContext.AGENTS.md",
          "description": "Relative path within the workspace to the generated Agent Context instructions file to update"
        }
      }
    }
  },
  "scripts": {
    "vscode:prepublish": "npm run compile",
    "compile": "tsc -p ./",
    "watch": "tsc -watch -p ./",
    "lint": "eslint src --ext ts",
    "test": "npm run compile && node --test test/**/*.test.mjs"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "@types/vscode": "^1.85.0",
    "typescript": "^5.3.0"
  }
}
